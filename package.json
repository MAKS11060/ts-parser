{
  "type": "module",
  "name": "@maks11060/parser-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "publish": "pnpm pack -r",
    "fmt": "dprint fmt",
    "lint": "dprint check",
    "prepare": "git config core.hooksPath .hooks"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "tsdown": "catalog:",
    "tsx": "^4.22.3",
    "typescript": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.3.0",
      "onFail": "download"
    }
  }
}
